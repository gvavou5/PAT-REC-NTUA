In order to run lab3prop you should place the .m file in a folder which will contain:
1)MusicFileSamples folder
2)MIRToolbox1.6.2 (the whole toolbox with subfolders)
3)deltas.m function
4)kriAlpha.m function

Then the script will create its own directories and give you all the expected outputs as presented at the current report.